name: Action on Pull Request to generate Kong Config from Spec and Validate it

on:
  pull_request:
    branches:
      - master

jobs:
  deploy_to_kong:
    runs-on: self-hosted
    name: Generate Kong config from Spec and Validate config
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup inso
        uses: kong/setup-inso@v2
        with:
          inso-version: 10.3.1
      - name: Check deck version
        run: inso --version
      - name: Generate Kong declarative configuration from Spec
        run: inso generate config ./openapi/orders.yaml --type declarative > kong2.x.yaml
      - name: Setup deck 
        uses: kong/setup-deck@v1
        with:
          deck-version: 1.7.0
      - name: Check deck version
        run: deck version
      - name: Convert configuration for 3.0
        run: deck convert --from kong-gateway-2.8 --to kong-gateway-3.9 --input-file kong2.x.yaml --output-file kong3.x.yaml
      - name: Validate Kong declarative configuration
        run: deck validate -s kong3.x.yaml
